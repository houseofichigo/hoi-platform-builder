Build-specific UI support lives here when it is factored out of route modules.

This folder is protected by `docs/source-tree-contract.md`; do not delete it to
silence import or build failures. Restore missing dependencies instead.
